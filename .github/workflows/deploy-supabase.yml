name: Deploy Supabase (Rubba)
on:
  push:
    branches: [main]
    paths:
      - "supabase/**"
      - ".github/workflows/deploy-supabase.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: DB push + functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: gdgseklsdpybhzkbzaeg
        run: |
          if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then echo "No token"; exit 0; fi
          npx supabase@2.20.12 link --project-ref "$SUPABASE_PROJECT_REF"
          npx supabase@2.20.12 db push --linked || true
          npx supabase@2.20.12 functions deploy payment-init --project-ref "$SUPABASE_PROJECT_REF" --use-api || true
          npx supabase@2.20.12 functions deploy payment-webhook --project-ref "$SUPABASE_PROJECT_REF" --use-api || true
          npx supabase@2.20.12 secrets set --env-file .env.example 2>/dev/null || true
